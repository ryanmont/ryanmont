<p id="notice"><%= notice %></p>
<div style="background-color:#f6f6f6">
<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Title</th>
      <th>Price</th>
      <th>Sale price</th>
      <th>Image</th>
      <th>Description</th>
      <th>Discount</th>
      <th>Other</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.id %></td>
        <td><%= product.title %></td>
        <td><%= product.price %></td>
        <td><%= product.sale_price %></td>
        <td><%= product.image %></td>
        <td><%= product.description %></td>
        <td><%= product.discount %></td>
        <td><%= product.other %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<hr>
<h3> Export Products via CSV & XLS</h3>
<br>


 <button >
<%= link_to "Download Products to CSV ", products_path(format: "csv") %>
</button>
  <br>
  <br> 
  <button> 
<%= link_to "Download Products to XLS", products_path(format: "xls") %>
</button>

<br> 
<hr>
<!-- Import CSV -->

<h3> Import Products via CSV </h3>
<br>

<%= form_tag import_products_path,  multipart: true do %>

<%= file_field_tag :file %>

<br>
<br>

<%= submit_tag "Import CSV"%>

<%end%>

<br>
<hr>
<%= link_to 'New Product', new_product_path %>
